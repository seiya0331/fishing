<div>
  <%= form_with model: [tweet, tweet_comment], url: tweet_tweet_comments_path(tweet_id: tweet.id) do |f| %>
    <%= f.text_area :comment, rows: '5',placeholder: "コメントをここに" %>
    <%= f.submit "送信" %>
  <% end %>
</div>